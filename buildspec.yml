version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
      - npm install -g @angular/cli

  build:
    commands:
      - ng build --configuration production

artifacts:
  files:
    - '**/*'